#include "client.h"

void app_main(void) {
    // nvs_init();
    // wifi_init_sta(WIFI_SSID, WIFI_PASSWORD);
    // ESP_LOGI(TAG, "Conectado a WiFi!\n");
    // socket_tcp();
    char* message = get_message(0, 1);
}